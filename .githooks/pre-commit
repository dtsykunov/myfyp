#!/usr/bin/env sh
set -eu

repo_root="$(git rev-parse --show-toplevel)"
cd "$repo_root"

echo "[pre-commit] Running lint and tests via Nix dev shell..."
nix develop --command sh -c "cd api && ./scripts/run-lint.sh && ./scripts/run-tests.sh"
